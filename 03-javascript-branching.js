// if:
// if (<condition>) {
// 
/// }
// <condition> will consist of comparison operators and logical operators
//
// comparsion are: >, <, >=, <=, ==, ===, !=, !==

// given two numbers, return the larger one
let n = 3;
let m = 4;

function getLargerNumber(a, b) {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}


